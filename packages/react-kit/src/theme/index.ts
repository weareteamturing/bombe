import {
  breakpoints,
  color,
  gradient,
  radii,
  space,
  fontWeights,
  fontSizes,
  lineHeights,
  opacity,
  elevation,
} from '@teamturing/token-studio';

const theme = {
  breakpoints,
  space,
  fontWeights,
  fontSizes,
  lineHeights,
  radii,
  colors: color,
  gradients: gradient,
  shadows: elevation,
  opacity,
};

export default theme;
